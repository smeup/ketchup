// copy here a big data
const bigData = {};

// console.log('number of columns', bigData.columns.length);

const basicData = document.getElementById('basic-data');
if (basicData != null) {
    basicData.data = bigData;
}
