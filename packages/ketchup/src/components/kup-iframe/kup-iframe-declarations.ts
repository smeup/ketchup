/**
 * Props of the kup-iframe component.
 * Used to export every prop in an object.
 */
export enum KupIframeProps {
    buttonData = 'Props of the button (when isButton is set to true).',
    isButton = 'The component will be rendered as a button, which opens the link associated to the iframe in another tab when clicked.',
    src = 'The address the iframe should be referencing to.',
}
